# WORKFLOW — {{PROJECT_NAME}} pipeline spine

> The step-by-step spine of the project: what each step does, its output, its lead
> role, and where the work fans out into concurrency. Loaded once per session (boot
> set). Role *detail* lives in ROLES.md + the sub-agent SPs; this doc is the spine.

## {{pipeline emoji}} The spine (step by step)
| Step | What it does | Lead sub-agent | Executes via (function/CLI) | QA gate | Output |
|---|---|---|---|---|---|
| ① {{intake}} | {{identify the unit of work / resolve the task id}} | `{{agent}}` | `{{core}} {{cmd}}` | `{{core}} qa {{step}}` | {{output}} |
| ② {{…}} | {{…}} | `{{agent}}` | {{`{{core}} {{cmd}}` or `[artisanal]`}} | {{…}} | {{…}} |
| … | | | | | |
| ⑥ Final QA & ship | Sweep **all** steps' outputs (counts + thresholds) → green unlocks ship → execute the outward action (package / publish / launch). **Always the CEO's hand.** | CEO | `{{core}} validate` → `{{core}} ship` | `validate` **is** the gate | the shipped product |
| ⑦ Reflection & finalize | Review this cycle's flaws + efficiency → attribute root causes → dispatch `dev-maintainer` to upgrade → consolidate → **report to the chairman** (shipped / cost / healed / org changes). **Always the last step.** | CEO | — (judgment) | — | `reflections/` + updated docs + the chairman report |

> **`[artisanal]` = industrialization debt** (Philosophy 3): the model still improvises
> this step's execution. Allowed at birth; tracked here and retired via `/self-reflection-cli`.

> **The baton between steps is a handle, not a payload.** Pass `task_id` + the
> smallest index needed (`build_dir`, `scene_index`, `prompt_id`, …); let the
> CLI/sub-agent read its own inputs from disk. Never paste long text (scripts, SPs,
> scene JSON) through chat.

## 🔀 Fan-out points (parallelism — Philosophy 2)
Express concurrency *here*, at the seams, not as per-agent bookkeeping:

```
{{ASCII: which steps run in parallel once they unblock}}
e.g.  after script-ready ──┬─▶ structure
                           ├─▶ metadata        (independent → fan out at once)
                           └─▶ thumbnail
      structure ──▶ scene prompts ──▶ [scene 1..N images]   (homogeneous → batch, M in flight)
```

- **Independent steps** that unblock together → dispatch in one message.
- **Homogeneous batches** (N scenes / N pages) → run M concurrently, not all-or-one.
- **External-partner steps** (`external-bridge` roles in ROLES.md) are *asynchronous*:
  submit the contract, keep working on unblocked steps, collect the report later —
  never block the pipeline waiting on a contractor.

## ✅ The QA chain (Philosophy 10 — quality is a function, not an opinion)
- **Worker self-check — "no self-check, no report":** every sub-agent runs its
  step's QA command (`{{core}} qa <step> <task_id>`) *before* reporting done, and
  attaches the passing output to its report. A report without it is invalid.
- **CEO final QA:** before ship, the CEO runs `{{core}} validate <task_id>` — one
  deterministic sweep of all outputs. Green unlocks `{{core}} ship`.
- **Red = repair work:** a failing check self-heals (Rule 8): fix → resume the same
  task id → re-check. Never lower a gate to pass it — loosening a threshold is an
  org-level change the CEO must decide explicitly and record.
- **Subjective residue** (taste / brand fit code can't measure) → a reviewer role or
  the CEO's spot-check, criteria in `playbooks/{{quality_gate}}.md` — the explicit
  exception, never the default. When the review has **multiple axes** (e.g. quality
  vs spec-compliance), dispatch one reviewer per axis in parallel and keep the
  verdicts side by side — merging them lets one axis mask the other.

## ⏸️ Gates (where the CEO must verify before moving on)
- {{gate 1, e.g. quality gate before publish → playbooks/quality_gate.md}}
- {{gate 2, e.g. the #1 iron-law check → CONSTITUTION.md}}

## 🔁 Reflection loops (both close here)
- **Loop A — per-cycle:** the final step writes a dated note to `reflections/`
  (permanent; never inside a build dir that gets cleaned).
- **Loop B — cross-session:** `/finalize-session` rewrites `NEXT_SESSION.md`;
  scheduled jobs drop analytics into `reports/`, which the next `/start-session` consumes.
> Detailed reflection procedure → `documentation/playbooks/reflection_playbook.md`.

## 🛠️ Production commands (the deterministic CLI does the executing)
{{the project launcher + the key subcommands, one line each — e.g. `./{{core}} <cmd>`.
The CEO calls these; it does not re-derive their logic each time. Full reference → the CLI's own --help / README.}}
