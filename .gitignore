# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Local experiments — keep the repo to the skill itself
/scratch/
/tmp/

# Local-only maintainer guide (private context, never published)
/CLAUDE.md
